import { Routes } from '@angular/router';
import { AuthResetPasswordComponent } from 'app/modules/auth/change-password/change-password.component';

export default [
    {
        path: '',
        component: AuthResetPasswordComponent,
    },
] as Routes;
